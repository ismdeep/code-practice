#include <stdio.h>
#include <stdlib.h>

int main()
{
	int a, b, c, max_value;
	scanf("%d%d%d", &a, &b, &c);
	printf("%d %d %d\n", a, b, c);

	if (a > b)
	{
		m = a;
	}
	else
	{
		m = b;
	}

	if (m > c)
	{
		m = m;
	}
	else
	{
		m = c;
	}

	return 0;
}
